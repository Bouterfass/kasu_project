<p> Salut <%= @user.name %>, modifie ton profil </p>




<ul>
<% @user_collection.each do |item| %>
    <li> <%= item.manga.title %> <%= item.manga.author %> <%= item.id %>  <%= link_to "Supprimer de ma bibliothèque", library_item_path(item.id), method: "delete",  :data => {:confirm => 'Are you sure?'} %></li>

<% end %>

</ul>